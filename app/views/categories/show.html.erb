<main class="shadow-sm">
  <%= render 'global_section' %>
  <div class="container">
    <div class="col-md-12">
      <h1 class="h4"><strong class="text-dark">Kategori : <%= @category.name %></strong></h1>
    </div>
    <div class="row">
      <div class="col-md-9">
        <%= render 'show_section' %>
      </div>
      <div class="col-md-3">
        <%= render 'layouts/global_sidebar' %>
      </div>
    </div>
  </div>
</main>
